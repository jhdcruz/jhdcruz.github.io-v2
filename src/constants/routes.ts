import { Pages } from '../types/Paths';

const routes: Pages[] = [
  { path: '/', name: 'home' },
  { path: '/projects', name: 'projects' },
  { path: '/about', name: 'about' }
];

export default routes;
